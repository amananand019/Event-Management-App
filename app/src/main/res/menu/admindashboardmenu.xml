<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/button_nav_add"
            android:title="Upload"
        android:icon="@drawable/ic_add_post"
        app:cnb_backgroundColor="#2196F3"/>
    <item android:id="@+id/bottom_nav_home"
        android:title="Home"
        android:icon="@drawable/ic_home"
        app:cnb_backgroundColor="#ffd700"/>
    <item android:id="@+id/bottom_nav_history"
        android:title="History"
        android:icon="@drawable/ic_history"
        app:cnb_backgroundColor="@color/pink"/>
    <item android:id="@+id/bottom_nav_profile"
        android:title="Profile"
        android:icon="@drawable/ic_settings"
        app:cnb_backgroundColor="#7CDEC7"/>
</menu>